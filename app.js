document.getElementById('risk-form').addEventListener('submit', function(e) {  e.preventDefault();  const age = parseFloat(document.querySelector('input[name="age"]').value);  const gender = parseInt(document.querySelector('select[name="gender"]').value);  let score = age * 0.5 + gender * 10;  if (score <= 33) { window.location.href = 'low-risk.html'; }  else if (score <= 66) { window.location.href = 'medium-risk.html'; }  else { window.location.href = 'high-risk.html'; }});document.getElementById('risk-form').addEventListener('submit', function(e) {  e.preventDefault();  const age = parseFloat(document.querySelector('input[name="age"]').value);  const gender = parseInt(document.querySelector('select[name="gender"]').value);  let score = age * 0.5 + gender * 10;  if (score <= 33) { window.location.href = 'low-risk.html'; }  else if (score <= 66) { window.location.href = 'medium-risk.html'; }  else { window.location.href = 'high-risk.html'; }});